function findSumformarrary(arrray){
    let dodaimang= array.length

    for(i=0;i<dodaimang;i++){
        let sum = sum + array[i]
    }
    return sum;
}

function find_max_in_array(array){
    let max = 0;
    for(i=0;i<array.length;i++){
        if (array[i]>max){
            max = arrary[i];
        }
    }

    return max;
}